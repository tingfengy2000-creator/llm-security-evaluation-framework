# 私有仓库公开风险审计

审计日期：2026-07-16。审计范围为当前 Git 工作树与全部可达 Git 提交，不展示任何敏感原文。

## 可见性结论

GitHub 未登录公开 API 对该仓库返回 404；远端地址与本地可认证 Git 访问均存在。据此确认仓库当前为 **Private**。因此本轮不触发公开仓库的阻断流程，也不修改仓库 visibility。

## 扫描摘要

| 项目 | 结果 | 解释 |
| --- | ---: | --- |
| 可达历史提交 | 22 | 已扫描严格凭据模式与标记模式 |
| 严格凭据值匹配 | 0 | 未发现 `gsk_`、`sk-` 或带长 token 的 `Bearer` 值 |
| 凭据标记文件修订 | 549 | 主要是教学文档、脚本和安全规则中的术语，需人工语义复核 |
| 含绝对路径的当前文本文件 | 116 | 主要来自历史实验命令、报告和日志 |
| 对话导出 | 2 | 不应进入公开发布面 |
| 二进制候选文件 | 14 | 包含面试资料渲染件等，公开前需逐件隐私审查 |
| 原始运行 trace 候选 | 30 | 包含代理 stdout/stderr 等，不应进入公开发布面 |

## 风险判断

仓库没有发现严格格式的真实 API Key，但不能因此视为可直接公开。绝对路径、对话导出、原始运行日志、HTML 报告、二进制材料和完整攻击/输出证据都可能造成隐私、可识别性或不必要的攻击细节暴露。

## 公开策略

采用：

```text
Private Research Workspace
    +
Public Artifact Repository 或 public-release branch
```

公开面只允许：源码、合成脱敏数据、可复现配置、小型脱敏结果、README、LICENSE、CITATION.cff、SECURITY.md、ETHICS.md、数据卡、模型卡、最小 Demo 和 CI。

## 发布前阻断条件

- 对话导出、私人项目书、个人文档、截图和原始 trace 未排除；
- 当前树或全部 Git 历史发现严格凭据值；
- 绝对路径、用户名、邮箱或 provider 原始请求未脱敏；
- 未完成第三方数据、模型和工具的 license 审查；
- 未在独立 clone 从零复现公开包。

## 后续动作

公开前新建独立 `public-release` 分支或仓库，执行全历史 secrets 扫描、隐私扫描、artifact allowlist、许可证核对与干净环境复现。审计报告只记录类别、数量和处理结论，不重复写入检测到的敏感原文。
