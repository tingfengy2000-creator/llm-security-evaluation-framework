# 实验结果与结论边界

- Stage 3：PromptInject 在真实 Groq 上 ASR 100%，Base64 detector PASS。
- Stage 4：两条 smoke 中 baseline ASR 50%，full guard ASR 0%，但均在输入侧拦截。
- Stage 4.1：input-only、output-only、full-guard 在两条 smoke 上 ASR 0%；output-only
  两条都调用了 Groq 并保存 raw hash。
- Stage 5：离线 mock 88 Attempt 验证矩阵和报告链，不代表 Groq 安全性。
- Stage 5 Paper：A1-A6 数据与确定性框架首先离线验证；真实 API 状态必须单独记录。

最新 Stage 5 Paper 离线 run 为 `20260701T081320Z-c29f39`：22 个样本、88 个 Attempt，
ASR 95.83%、GBR 94.44%、官方 Detector Coverage 8.33%、Over-block 0%。高 ASR
来自故意复述风险标记的 mock，用于展示 rule-based baseline 的覆盖缺口。

所有结果只适用于当前样本、当前模型版本和当前 rule-based baseline。
