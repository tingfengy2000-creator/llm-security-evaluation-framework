# D1 Canary Phase2 — R4-codex

Continue in the **same independent R4-codex session** that returned the locked full and targeted Phase1 reviews. The coordinator has accepted both primary targeted Phase1 returns and now supplies the frozen Phase2 V4 packet. Do not inspect the other reviewer's work.

Read only these supplied files: `PAPER1_FORMAL_D1_CANARY_PHASE2_V4.json`, `PAPER1_FORMAL_D1_BLIND_REVIEW_IMPORT_SCHEMA_V2.json`, `PAPER1_FORMAL_ANNOTATION_GUIDE_V4.md`, `PAPER1_FORMAL_ANNOTATION_GUIDE_V4_1_CLARIFICATION.md`, and this prompt. Review the 24 opaque IDs in packet order. Use only each row's frozen E1/E2 excerpts and explicitly supplied official URLs; do not search for additional evidence. Do not access the project repository, handoff directory, construction records, identity mapping, hidden roles, Expected/GT, Owner materials, or any previous reviewer's return. Your own Phase1 interaction is the only permitted prior review context.

Independently fill exactly the Phase2 schema fields. Keep document-version, authority and substantive claims separate. Publication/adoption/order/date alone does not automatically make a version claim; ordinary operational actors are not automatically authority claims. For a confirmed factual conflict already visible inside the candidate, use `ZERO_EXTERNAL_EVIDENCE_REQUIRED`; otherwise use the Guide V4 evidence decision rule. `evidence_selection` records evidence actually used, not the minimum. A factual conflict by itself is not `phase2_issue`; a session or routing defect is a run-level report to the coordinator, not a candidate-level issue. Give a concise evidence-bound `phase2_reason` without guessing hidden design intent.

Return one raw JSON array of exactly 24 objects, in supplied ID order, with the schema's exact keys and legal English enums. Do not rewrite the supplied candidate or evidence. Submit the raw response to the coordinator for a separate byte lock. This is pre-annotation construction QA, not Ground Truth.

Codex execution boundary: remain in the same isolated projectless Phase1 task. Open only the five files explicitly supplied for Phase2. Do not run Git commands, filesystem scans or tool searches for project files. If other project or hidden-answer context becomes visible, stop and report that at run level; the return cannot count as an independent gating review.
