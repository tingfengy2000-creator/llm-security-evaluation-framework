# 复现命令

## 离线回归

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\llmProject\scripts\run_stage5_paper_regression.ps1"
```

## 单类攻击

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\llmProject\scripts\run_stage5_paper_single_attack.ps1" -AttackId A4 -Provider mock
```

## 真实 smoke

在当前 PowerShell 会话配置环境凭据后运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\llmProject\scripts\run_stage5_paper_smoke.ps1" -Provider groq
```

## Git 上传前检查

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\llmProject\scripts\git_preflight.ps1"
```
