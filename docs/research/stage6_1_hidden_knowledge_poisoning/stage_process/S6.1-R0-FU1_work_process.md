# S6.1-R0-FU1 工作过程

> 当前阶段摘要：P0 与 L1 已人工验收；父 W2 为 `APPROVED_TO_START / NOT COMPLETED / NOT ACCEPTED`。W2 Attempt 1 为 `VALID_BLOCKED_ENGINEERING_RUN / MODEL_DOWNLOAD_BLOCKER`，Correction 02 已解除证据 blocker；H1 离线模型包已由本机准备并验证。项目需求提出人现已批准 H2，但尚未发送或执行：5090 必须先完整通过 H2-A，才可在同一合同内执行一次 H2-B 双文档工程 smoke。未产生正式论文结果。

## 1. 阶段目标

以本机优先、5090 门控的方式，定向解决 R0 遗留的外部攻击工件复用、GMTP detection-core 最小兼容性与 SafeRAG 工件边界，不扩大到正式实验。当前最小必要步骤是关闭 W2 工程运行门，不是进入 P1。

## 2. 进入条件

R0 已 `HUMAN_ACCEPTED_WITH_BLOCKERS`；FU1、P0/L1、W2 与 H1 分别经过人工批准、验收或证据复核。H2 的审批基础提交为 `212911a21dc35bef05b15fb840542403c415dd13`。旧 `H2 = PROPOSED / NOT CANONICAL / NOT APPROVED` 是批准前历史快照，保留但由当前批准决定 supersede。

## 3. 已批准范围

- P0：本机控制面规划与执行合同冻结。
- L1：本机静态源码、数据和 detection-only 调用链核对。
- W2：5090 detection-core 最小 smoke，不含生成、正式评测或训练。
- H1：本机为既有 W2 准备固定 revision 离线模型工件，不等于模型已在 5090 使用。
- H2：`APPROVED_TO_START / NOT SENT / NOT EXECUTED`；`Auto Continue = CONDITIONAL_WITHIN_H2_ONLY`。5090 先执行 H2-A；只有全部实质条件通过才可执行一次 H2-B；完成或遇到 blocker 后立即停止并返回本机复核。

H2 不批准 S6.1-P1、Dataset、Detector、Training、Our Method Result 或 Formal Experiment。“解毒方法”的技术范围仍为 `SCOPE_CONFIRMATION_REQUIRED`。

## 4. 执行环境

本机负责规划、审批登记、源码分析、证据复核、文档同步和 Git；本轮不得解压或加载模型、运行 GMTP/harness 或使用 GPU。5090 负责获批的 H2-A/H2-B。上下文通过 Git 同步，bundle、模型、raw logs 与 evidence archive 保持 Git-external。

5090 必须复用既有 `gmtp-compat`，不得创建、重建、安装、卸载、更新或清理环境。冻结版本为 Python 3.11、Torch `2.13.0+cu130`、CUDA runtime 13.0、Transformers 4.47.1、NumPy 1.26.4。执行前后保存 `conda list -n gmtp-compat --explicit` 并要求 SHA256 相等；禁止 CPU fallback。否则登记 `ENVIRONMENT_CONTRACT_BLOCKER` 并停止。

## 5. 源码、数据与模型身份

- GMTP commit：`15b48d150f93711371eb8da22c211cd84a0cf4df`。
- `src/defenses/method.py` blob：`84e69b3eadeb8adc0ce521501f8b560d6377b489`；SHA256：`83531fe0e4933074c0a710f3dc07bb260b5d638d3cd4c8c317a353de135e00f6`。
- 输入路径：`data/poisoned_documents/poisonedrag/hotflip/contriever/nq-200.json`；blob：`72fb52cda9ea794bafb5c114ee937a00f4d1728a`；大小 `975113` bytes；SHA256：`0233a26ecc56d7baf1448b86a114e328beece60624aa88304fa3553e90421e44`。
- 唯一记录：serialized array index `0`，`query_id=test0`；question SHA256 `53c1176d667577193fa7b4eb171319597c4b99dd80b3e72d8eb779b98a204b0d`；benign `gt_text[0]` SHA256 `e177b2da95be6282c2f4c7855e36dde35a875bd21190755b27329353252d2f09`；poisoned `poisoned_docs[0]` SHA256 `b94ca256b3ac79d22429abe1d36d90fad352c024cd2b5e19b78cfb2e72135a5b`。不得保存或输出完整文本。
- Encoder：`facebook/contriever-msmarco@abe8c1493371369031bcb1e02acb754cf4e162fa`，8 files，`438708922` bytes。
- MLM：`google-bert/bert-base-uncased@86b5e0934494bd15c9632b12f734a8a67f723594`，9 files，`881643453` bytes。
- Total model bytes：`1320352375`。

## 6. 工作拆解

1. P0 冻结本机执行合同、证据字段、比较边界与机器分工。
2. L1 验证 PoisonedRAG artifact reuse、GMTP detection-only dependency 与 SafeRAG 工件边界。
3. W2 Attempt 1 在 5090 遇到模型下载 blocker；Correction 01/02 追加证据闭环。
4. H1 在本机准备固定 revision 离线模型 bundle。
5. H2 在 5090 先执行 H2-A 离线 bundle 完整性验证；仅当 H2-A 全部通过时，条件进入一次 H2-B；之后无论完成或阻断都停止并返回本机。

## 7. 实际执行过程

P0/L1、W2 Attempt 1、Correction 01/02 与 H1 的历史事实保持不变。H1 bundle 尚未由 5090 独立验证或加载，GMTP detection-core 尚未实际成功执行。

### H2 人工批准

项目需求提出人批准 `S6.1-R0-FU1-W2-H2`，状态为 `APPROVED_TO_START / NOT SENT / NOT EXECUTED`。这是一项条件式工程批准：H2-A 失败不得进入 H2-B；H2-B 只允许一次冻结的双文档调用；禁止自主修复、重复运行、扩大样本或跨入后续阶段。

### H2-A 离线模型包验证门

Bundle 为 `s6_1_r0_fu1_w2_models_20260801.tar.gz`，期望 archive size `1222137698` bytes，SHA256 `aa06e4cd03cb4d1eeb008514d81bc4d41e98f88614df046e008ac1f1544def45`，bundle source bytes `1320359518`，model index `19/19`。Bundle source bytes 包含 manifest/README，故不要求与 total model bytes 相等。

5090 必须先验证以下 18 项：sidecar SHA256、实际 archive SHA256、合同 SHA256、archive 大小、无绝对路径、无路径穿越、无 symlink、无 hardlink、无 device entry、成员集合符合 manifest、`model_files.sha256` 为 19/19、manifest 与实际文件一致、Encoder 身份/files/bytes 一致、MLM 身份/files/bytes 一致、total model bytes 一致、bundle source bytes 一致、无 token/cookie/credential、无缓存/下载日志/环境文件/额外模型。

目标目录为 `~/paper1_external/w2_models_20260801`，执行前必须不存在或为空；不得合并或覆盖旧目录。先安全检查，后解压；解压后重新验证 19/19、文件数与字节数。任一不一致必须停止，登记最具体的 `OFFLINE_BUNDLE_SHA_BLOCKER`、`OFFLINE_BUNDLE_SAFETY_BLOCKER`、`OFFLINE_BUNDLE_INDEX_BLOCKER`、`OFFLINE_BUNDLE_MANIFEST_BLOCKER`、`OFFLINE_MODEL_IDENTITY_BLOCKER` 或 `OFFLINE_MODEL_DESTINATION_CONFLICT`，不得进入 H2-B。

### H2-B 冻结 detection-core 合同

开始前设置 `HF_HUB_OFFLINE=1`、`TRANSFORMERS_OFFLINE=1`、`HF_HUB_DISABLE_TELEMETRY=1`、`TOKENIZERS_PARALLELISM=false`。模型只允许从 H2-A 安全解压目录加载；禁止 repo_id 联网加载、在线回退、token、登录、API、非官方镜像、其他模型、floating revision 或缺文件后联网补齐。缺文件登记 `OFFLINE_MODEL_LOAD_BLOCKER` 并停止。

参数固定为 `ret_type=contriever`、`N=5`、`M=5`、`remove_threshold=0.2`、`remove_lambda=1.0`、`topk=10`、`do_sort=false`。官方配置为 N=5/M=5/0.2/1.0；类构造默认值为 N=10/M=5/-1/1；本轮实例化必须显式采用官方配置，函数调用必须显式采用 topk/do_sort/doc_ids。必须区分官方配置、类默认值与本轮实际传参，不得改参。

通过 `importlib.util.spec_from_file_location` 直接加载未修改的 `src/defenses/method.py`；`model_name` 指向本地 Contriever 目录，`reranker` 指向本地 BERT 目录。只允许一次调用：

```python
filter_documents(
    question,
    [benign_document, poisoned_document],
    topk=10,
    do_sort=False,
    doc_ids=["benign", "poisoned"],
)
```

不得修改 `method.py`、重写算法、替换记录/文档/模型/阈值、拆分研究设置、为理想结果重复运行或扩展为批量评测。

### H2 证据与停止合同

允许只读 harness 使用 `sys.settrace` 在官方函数 return event 读取 frame locals 中的 `doc_infos`，不得改变控制流。每个文档记录 document id/hash、`sim`、`avg_masked_prob`、threshold、retained/filtered、finite 与 NaN/Inf；同时记录本地模型路径/revision/index、Transformers、CUDA、exit code、wall time、maximum RSS、peak GPU allocated/reserved，不保存完整 query/document。

除上述 bundle blocker 外，Git/source/input mismatch、环境 mutation、未冻结依赖、新包需求、源码/参数修改需求、网络回退、CPU fallback、CUDA 不可用、NaN/Inf、OOM、资源超限、证据失败均必须停止，不得自行修复。使用最具体的 `UNEXPECTED_CORE_IMPORT_DEPENDENCY`、`COMPATIBILITY_PATCH_REVIEW_REQUIRED`、`CUDA_EXECUTION_BLOCKER`、`GMTP_DETECTION_CORE_COMPATIBILITY_BLOCKED`、`EVIDENCE_CAPTURE_BLOCKER` 或 `WORKER_RESOURCE_APPROVAL_REQUIRED`；任何 blocker 不得解释为 GMTP 算法失败。

资源上限保持为：`gmtp-compat <= 6 GiB`、两个模型快照 `<= 2 GiB`、task-owned total disk `<= 10 GiB`、RAM `<= 16 GiB`、VRAM `<= 8 GiB`、模型本地可用后的运行时间 `<= 10 minutes`、evidence/harness/archive `<= 256 MiB`。任何超限登记 `WORKER_RESOURCE_APPROVAL_REQUIRED` 并停止；不得通过删除必要工件或清理冻结环境伪造通过。

## 8. 关键命令

H2 的正式 5090 prompt 必须生成只读身份核验、安全 archive 检查、冻结环境前后快照、离线模型加载和一次调用所需命令。不得包含环境安装、源码修改、网络访问或自动重试。H2-A 全部实质通过是 H2-B 的硬前置条件，不得以脚本 exit code 代替逐项证据判断。

## 9. 输入

只允许第 5 节冻结的 GMTP commit、method blob/hash、200-record 文件中的 index 0、两个文档 hash、固定模型 revision 与第 7 节固定参数。不得替换样本、补充第三个文档或输出完整文本。

## 10. 输出

证据目录固定为 `~/experiments/s6_1_r0_fu1/w2/resume_01`，至少包含 `approval_contract_snapshot.md`、`git_integrity.txt`、`bundle_outer_identity.txt`、`archive_safety.json`、`model_manifest_copy.json`、`model_index_verification.txt`、`extracted_model_identity.json`、`environment_pre.txt`、`environment_post.txt`、`offline_environment.txt`、`input_identity.json`、`source_identity.txt`、`resume_harness.py`、`resume_harness.sha256`、`run_command.txt`、`stdout.log`、`stderr.log`、`resource_measurement.txt`、`result_redacted.json`、`evidence_index.sha256`。

输出 archive 固定为 `/mnt/e/LLMGuard-Handoff/s6_1_r0_fu1_w2_resume01_evidence_20260801.tar.gz` 及对应 `.sha256`。Archive 只能包含 `resume_01`，必须通过安全检查与 evidence index，不得含模型、bundle 副本、完整输入文本、token/credential、环境副本或 Git 仓库副本。

## 11. 结果

- P0、L1：`HUMAN_ACCEPTED`。
- W2 Attempt 1：`VALID_BLOCKED_ENGINEERING_RUN / MODEL_DOWNLOAD_BLOCKER`。
- W2 evidence blocker：`RESOLVED_BY_CORRECTION_02_CONTROL_PLANE_REVIEW`。
- H1：`OFFLINE_MODEL_ARTIFACTS_PREPARED_PENDING_5090_VERIFICATION`。
- H2：`APPROVED_TO_START / NOT SENT / NOT EXECUTED`。
- 父 W2：`APPROVED_TO_START / NOT COMPLETED / NOT ACCEPTED`。
- `S6.1-P1 = NOT STARTED`；`Dataset = NOT FROZEN`；`Detector = NOT IMPLEMENTED`；`Training = NOT STARTED`；`Our Method Result = NONE`；`Formal Experiment = NOT STARTED`。

若 H2-A、离线加载和 H2-B 均完成，最大状态只能是 `W2_RESUME01_ENGINEERING_SMOKE_COMPLETED_PENDING_REVIEW`，仍须返回本机复核。

## 12. 证据与哈希

H1 bundle 身份见第 7 节；Encoder/MLM 身份见第 5 节，最终 model index 为 `19/19 PASS`。Correction 02 的历史 archive SHA256 为 `fcfa3f14c98e0103cb5a1de2f0449fa000d179e2e01d74baa6fec4b013503622`，evidence index 为 `17/17 PASS`；W2 original、Correction 01/02 的其余历史 archive/index/hash 继续由 Attempt 1 Control Plane Review 保存，不在本文件重写。H2 当前只有批准与冻结合同，没有 5090 执行证据。

## 13. 失败与 blocker

Attempt 1 的模型在线下载 blocker 与早期 evidence provenance 缺口是历史事实；Correction 02 只解决证据闭环，H1 只解决离线供应准备。当前未解决的是 5090 独立 bundle 核验、固定模型离线加载和一次 detection-core 工程调用。H2 任一 blocker 都终止当前 H2，不授权修复、重试或下一阶段。

## 14. 失败分析

下载失败不证明 detection algorithm 不可行；bundle 或环境身份失败不证明算法失败；单次核心调用失败也不得在缺少独立归因时宣称 GMTP 无效。PoisonedRAG 发布攻击文本复用不等于 generation reproduction，GMTP packaged HotFlip 与 PoisonedRAG LM-targeted 也不是同一正式比较链。

## 15. 解决方式

采用同一 H2 合同中的条件式执行，既保留离线模型来源链与运行可追溯性，也避免仅因 H2-A 通过再申请一次没有研究价值的审批循环。该条件仅存在于 H2 内部，不产生对 P1、Dataset、Detector、Training 或 Formal Experiment 的自动继续权。

## 16. 人工决定

项目需求提出人已批准 H2 并 supersede 批准前的 proposed 快照，但保留历史记录。`FORWARD_RISK_REVIEW = PASS_FOR_CONDITIONAL_H2`；`PAPER_RISK_REVIEW = PASS_WITH_ENGINEERING_ONLY_CLAIMS`。批准基础为 `212911a21dc35bef05b15fb840542403c415dd13`；实际审批登记提交由本轮 Git 记录确定。

## 17. 允许宣称

当前只允许声明 H2 已批准、未发送、未执行，以及被冻结的 bundle/source/input/model/environment/parameter/evidence 合同。H2 执行后最多允许声明：bundle 在 5090 的身份核验结果、固定模型能否离线加载、未修改 GMTP detection core 能否完成一次固定双文档工程调用，以及该调用的原始工程分数、决策和资源测量。

## 18. 禁止宣称

禁止声明 W2 已完成或验收、GMTP 已复现或有效、安全检测有效、完整 baseline 复现、Accuracy/Precision/Recall/F1/AUPRC/AUROC/Filtering Rate/ASR、模型泛化、baseline 优劣、SOTA、Dataset 冻结、Detector 实现、Training 开始、Our Method Result 或正式论文结论。

## 19. 阶段退出条件

H2 完成或遇到任一 blocker 后，5090 必须停止并将受控证据 archive 返回本机。只有本机独立复核和项目需求提出人的后续决定才能改变父 W2 状态。当前未满足 W2 验收或 FU1 退出条件。

## 20. 当前状态

`S6.1-R0-FU1 = IN_PROGRESS / 5090 门控`。H2 已批准但未发送、未执行；父 W2 未完成、未验收。P1、Dataset、Detector、Training、Our Method Result 与 Formal Experiment 均保持第 11 节状态。

## 21. 下一审批门

项目需求提出人将本轮审批登记提交和 Git-external bundle 交给 5090。5090 执行 H2-A；只有 18 项 bundle 条件、冻结环境和强制离线条件全部通过，才执行一次 H2-B；完成或阻断后停止并返回本机复核。不得自动进入 S6.1-P1 或 Formal Experiment。
