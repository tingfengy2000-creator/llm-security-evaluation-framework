# P1-FDD-FAILURE-ANALYSIS-EVIDENCE-BLOCKER-01

Status: `HUMAN_DECISION_REQUIRED / Auto Continue=NO` (2026-09-22). This is a decision-ready evidence-gap record, **not** the requested completed failure-analysis report. No model was fitted and no frozen artifact was changed in this task.

1. **Issue ID:** `P1-FDD-FAILURE-ANALYSIS-EVIDENCE-BLOCKER-01`.
2. **Issue name:** Per-model sample-level OOF score/trace not persisted for S/E/P/T-only and Full-minus-view models.
3. **Discovery task:** `P1-DOCUMENT-DETECTOR-FAILURE-ANALYSIS-AND-SCALE-HYPOTHESIS-REFINEMENT-01`, input preflight, before analysis output generation.
4. **Current facts by evidence level:** `OBSERVED_FACT`: the immutable V1.1 run namespace has ten indexed files and zero hash mismatches; `result_hash_manifest.json` SHA256 `e788af67ee1a67d8635ddb11a9a27feeb3d7c89009a87bb3d1788ddfd90737af`. Its sole sample-level OOF file contains 72 Full SEPT predictions (SHA256 `eed87a32979cb2d1c85bbd67584cf5a3d2c9cd32789af933758530e65ba9d423`). View-comparison and ablation JSONs contain aggregate `metrics`, aggregate `matched`, and `feature_count`, but no per-sample or per-group score array. Coefficient JSON contains 24 Full SEPT fold vectors, not other models. A bounded `E:\LLMGuard-Handoff` filename search found no other detector-view OOF file. `SOURCE_DERIVED_FACT`: committed execution code `d9956c4` computes each model's score in memory, serializes only Full SEPT OOF, and strips per-group rows from compact view/ablation outputs. `INFERENCE`: the nine model OOF predictions existed transiently during execution, but eight models' individual values cannot be recovered from the locked deliverables without an additional source or a new fit. `UNKNOWN`: whether Owner holds an unindexed, separately preserved exact run-time copy outside the searched handoff tree.
5. **Affected constraints:** Current task sections 4, 15–16, 18, 20–22 and 44 require frozen view/ablation sample OOF, 24-group Full-vs-Full-T/P score shifts, view-specific hardest groups and HKP/S/domain matched analysis. Sections 1, 3 and 45 forbid retraining, replacing OOF, or modifying frozen results. PO-MHEP requires stopping on incomplete raw result lineage.
6. **Why now:** Any Full-vs-minus-view *per-group* HELPED/HURT count or view-specific group explanation generated from aggregate AUROC/AUPRC/margins would fabricate information. This gap must be resolved before such analysis and before claiming the task's complete QA/closeout.
7. **Downstream risk:** Engineering: score-shift pipeline cannot validate inputs. Architecture: wrong P/T diagnosis could drive an unjustified redesign. Experiment/paper: aggregate differences could be mislabeled group-level or causal evidence. Reproducibility: prior code commit and input SHA are known but missing serialized model outputs prevent independent review of eight score vectors. Time/resource: a reconstruction fit would consume extra CPU time and create a new evidence event. Safety/license: no new source or model is needed, but any new run must preserve the frozen license/input boundaries.
8. **Options:**

   - **A — Supply an existing exact artifact.** Action: Owner supplies any authentic, timestamped eight-model OOF/per-group score file from the original run; verify identity, hash, 72×8 parity and lineage. Advantage: no retraining and preserves present prohibition. Disadvantage: no such file was found locally; it may not exist. Cost: low if present, otherwise search time. Risk: identity/transport uncertainty. Later-stage impact: cleanest original-run analysis if verified. Paper impact: strongest provenance. Reversibility: additive, high.
   - **B — Separately authorize one additive evidence-reconstruction run.** Action: use exact frozen V1.1 inputs, 24 LOGO folds and LR configuration to re-fit only for all-model OOF serialization in a **new** namespace/commit, with explicit before/after hash and numerical parity against the original Full OOF and aggregate metrics; never overwrite or silently call it the original run. Advantage: supports the requested group analyses with auditable inputs. Disadvantage: changes the current `NO_RETRAINING` boundary and creates a second model-fit event. Cost: modest local CPU and documentation/QA work. Risk: environment or numerical drift, which must fail closed if parity differs. Later-stage impact: enables complete diagnosis without feature/GT edits. Paper impact: transparent, versioned secondary diagnostic, not a formal result. Reversibility: additive, high.
   - **C — Narrow the authorized task.** Action: analyze frozen Feature Matrix and Full SEPT OOF only, leave view-specific group/score-shift questions explicitly `NOT_EVALUABLE`. Advantage: no fit and quick partial scientific insight. Disadvantage: fails the original required Full-vs-Full-T/P and view-specific matched-group deliverables. Cost: low. Risk: an incomplete diagnosis could be mistaken for a complete explanation. Later-stage impact: weaker formal-scale hypothesis basis. Paper impact: limited development observation only. Reversibility: high; B could follow later by new approval.
9. **LOCAL recommendation:** First accept A only if an authentic pre-existing artifact is actually available. Otherwise choose B by explicit new Owner approval, with exact-code/input parity gates and a new additive evidence identity. Do not silently infer B from this recommendation; C remains a valid reduced-scope choice if no second fit is desired.
10. **Rationale and confidence:** High confidence in the missing score vectors within the indexed run and searched handoff files; moderate confidence that no copy exists elsewhere. Aggregate model metrics are insufficient to invert back to 72 individual scores or 24 group shifts. A separate exact-code re-fit is the only apparent reproducible recovery route if A is unavailable.
11. **Owner decision required:** Please choose A (provide the existing exact per-model OOF artifact), B (explicitly approve the additive reconstruction fit despite this task's no-retraining rule), or C (approve a partial feature/Full-only analysis and waive the listed deliverables). If B, confirm the new run must remain development-only and must stop on any original-result parity failure.
12. **Until decision:** Allowed: read-only hash/schema/source checks, risk analysis, preservation of this blocker and governance/Git records. Prohibited: model fit/re-fit, score interpolation, synthetic per-group claims, Full-vs-minus-view shift classification, feature/GT/Evidence/Registry/query changes, final scale hypothesis acceptance, formal result, and task-complete status.
