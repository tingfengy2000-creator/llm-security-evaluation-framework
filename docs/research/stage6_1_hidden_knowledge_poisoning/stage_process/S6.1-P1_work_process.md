# S6.1-P1 工作过程

> Paper 1 唯一 canonical stage process；只追加 P1 事实，不改写 LR1、R0 或 FU1 历史。

## 1. 阶段目标

在不采集真实数据、不训练模型、不执行正式实验的条件下，建立 Benchmark、标签隔离、分组、防泄漏、攻击描述、轻量解毒和运行清单的最小代码基础设施，并用纯合成中文 fixture 验证确定性工程合同。

## 2. 进入条件

- P1-R1 已由项目需求提出人接受为协议框架，Option B 已冻结。
- 数值参数仍为 `PENDING_PILOT_EVIDENCE`，P1 formal protocol 为 `NOT YET FROZEN`。
- 本轮只批准 PILOT0 基础设施，不批准真实数据、标注 Pilot 或 Formal Experiment。

## 3. 已批准范围

允许 schema、中央可见性、版本链/group identity、group-aware split、leakage validator、MutationSpec、12 类 hard negative、hard filtering、soft downweighting、missing-evidence abstention、RunManifest 与纯合成 fixture。禁止真实文档、私人数据、外部 API、自然语言攻击生成、外部模型、GMTP、GPU、Detector 训练、性能指标与正式实验。

## 4. 执行环境

- 执行机器：本机。
- `CODEX_RESOURCE_PRIORITY = 本机优先`；研究设计、代码、测试、Git 和证据复核不因累计消耗而迁移。
- `NO_LOW_VALUE_CHURN = ENFORCED`；不为格式、字段顺序或重复措辞建立额外提交。
- 未新增依赖，未使用网络、模型或 GPU。

## 5. 源码、数据与模型身份

- 基础提交：`4b0395584627636f5f13658a990614d8f39561eb`。
- schema：`paper1-pilot0-v1`。
- 数据：24 条明确声明的纯合成中文记录；没有真实或私人数据。
- 模型：`NONE`；Detector：`NOT IMPLEMENTED`。

## 6. 工作拆解

实现 `schema.py`、`visibility.py`、`groups.py`、`splits.py`、`leakage.py`、`attacks.py`、`hard_negatives.py`、`intervention.py`、`manifest.py` 与包导出；建立对应定向测试和唯一 P1 process；最后同步许可范围内的状态镜像。

## 7. 实际执行过程

首先以严格 dataclass/Enum 建立 canonical schema 和 hash；再实现 fail-closed visibility/leakage、传递闭包分组与 deterministic split；随后实现字段级攻击合同、hard-negative taxonomy、纯函数 intervention、abstention 和 manifest；最后建立 12 个 HKP × stealth 与 12 类 hard negative 的 fixture 并运行定向测试。

## 8. 关键命令

```text
PYTHONPATH=src python -m pytest tests/domains/retrieval/hidden_poisoning -q
PYTHONPATH=src python -m pytest tests/architecture -q
```

其余静态验收按任务冻结顺序执行；不重复运行无实质失败的完整测试。

## 9. 输入

输入仅包括已验收 P1-R1 框架、项目现有 retrieval namespace 和测试内构造的纯合成记录。任何 evaluator-only 字段名或值进入运行时 payload 都触发 `LABEL_LEAKAGE_BLOCKER`，不会静默删除后继续。

## 10. 输出

- 规范实现：`src/llmguard/domains/retrieval/hidden_poisoning/`。
- 定向测试：`tests/domains/retrieval/hidden_poisoning/`。
- 本过程文件以及允许范围内的简洁状态同步。
- 不输出 raw archive、模型、真实数据集或论文指标。

## 11. 结果

Schema、标签隔离、六类 identity 的传递闭包、可配置 group split、exact/normalized/identity leakage、HKP 字段 allowlist、12 类 hard negative、hard filtering、soft downweighting、missing-evidence abstention 和 engineering-only manifest 已实现。语义近重复扫描明确为 `NOT_IMPLEMENTED / FAIL_IF_REQUIRED`。

## 12. 证据与哈希

- Fixture：24 条纯合成记录。
- Fixture SHA256：`4f381451688150016b1a518895ad75149cfdfdac4cd512dd6062becba04b2ed0`。
- Targeted tests：final `41 passed`。
- Claims classification：`ENGINEERING_VALIDATION_ONLY`；`FORMAL_RESULT` fail closed。

## 13. 失败与 blocker

首次定向测试为 `38 passed / 2 failed`：软降权样例参数未实际造成预期排序反转，合成免责声明断言与原文相邻措辞不一致。两者均为测试契约问题，不是算法、依赖、标签隔离或数据 blocker。

## 14. 失败分析

归一化后样例 A 的分数为 1，风险惩罚 0.9，仍高于样例 B 的 0；因此旧断言数学上错误。免责声明包含“真实政策、个人或机构”，而旧断言错误要求不相邻字符串“真实个人”。

## 15. 解决方式

把显式 `lambda_value` 调整为能验证 rank change 的测试值并同步公式断言；将免责声明断言改为实际合成边界措辞；固定 fixture SHA 快照。窄修复后为 `40 passed`，随后补强嵌套 schema/visibility fail-closed 回归，最终定向测试 `41 passed`。

## 16. 人工决定

- `S6.1-P1-R1 = HUMAN_ACCEPTED_AS_PROTOCOL_FRAMEWORK`。
- `S6.1-P1 = APPROVED_FOR_PILOT0_INFRASTRUCTURE_ONLY / NOT FORMAL_EXPERIMENT`。
- `P1_NUMERIC_PARAMETERS = PENDING_PILOT_EVIDENCE`。
- 本轮不得自动批准任何后续数据、矩阵或正式计算任务。

## 17. 允许宣称

P1-R1 已作为协议框架验收；Option B 已冻结；PILOT0 的 schema、标签隔离、group/split、leakage、attack contract、轻量 intervention、manifest 与纯合成 fixture 工程验证已完成待审。

## 18. 禁止宣称

不得声称 Benchmark 已构建、真实数据已收集、标注一致性已验证、Detector 已实现、GMTP 已正式复现、Option B 有效、任何性能指标、240-group Pilot、Our Method Result、Formal Experiment、Paper Result 或 SOTA。

## 19. 阶段退出条件

代码、定向与全量架构测试、namespace/label、Ruff、MyPy、Markdown links、secret/private-path、raw artifact exclusion、protected history 和 `git diff --check` 全部通过；单一功能提交推送后，本轮停止并等待人工审查。

## 20. 当前状态

- `S6.1-P1-PILOT0 = COMPLETED_PENDING_REVIEW`。
- `REAL_DATA_PILOT = NOT APPROVED / NOT STARTED`；`240_GROUP_PILOT = NOT APPROVED / NOT STARTED`。
- `MINIMAL_MATRIX / FULL_MATRIX = NOT APPROVED`。
- Dataset `NOT FROZEN`；Detector `NOT IMPLEMENTED`；Training `NOT STARTED`；Our Method Result `NONE`；Formal Experiment `NOT STARTED`。

## 21. 下一审批门

项目需求提出人审查 PILOT0 工程证据，然后决定是否批准真实数据与标注 Pilot。该门已由 OR-023 的 PILOT0 最终验收和 PILOT1 公开来源可行性批准关闭；原文保留为历史快照。

### PILOT0 最终验收与 PILOT1 授权

- `S6.1-P1-PILOT0 = HUMAN_ACCEPTED / ENGINEERING_INFRASTRUCTURE_ONLY / CLOSED`。
- 验收身份：commit `c555e7da4e5593f72cbf062823feda6bc7798e58`；targeted `41 passed`；architecture `113 passed`；24 条 synthetic fixture SHA256 `4f381451688150016b1a518895ad75149cfdfdac4cd512dd6062becba04b2ed0`。
- `S6.1-P1-PILOT1 = APPROVED_TO_START / REAL_PUBLIC_SOURCE_FEASIBILITY_ONLY`；只允许本机执行公开中文版本链审计、受控候选构造和 Git-external 双盲标注包准备。
- 未授权 5090、模型、Detector、正式双人标注、240-group Pilot 或 Formal Experiment。

### PILOT1-A 公开来源审计

- 权威执行目录为 Git-external corrected run；首次零长度规范化来源尝试和一次瞬时采集中断均原样保留为非权威历史，不覆盖证据。
- corrected run 审计 `12` 条独立版本链、`3` 个领域且每领域 `4` 条链、`24` 份官方公开来源；原始与规范化 artifact 各 `24` 份，规范化最小长度 `918` bytes。
- 15 项 source audit gate 全部 `PASS`；source manifest `24/24`、chain relations `12/12`、acquisition log `24/24`。
- 条款未明确授权再分发，因此全部使用 `NOT_EXPLICITLY_VERIFIED / NOT_AUTHORIZED_FOR_REPUBLICATION / HASH_ONLY`；完整正文、网页和 PDF 均保持 Git-external。

### PILOT1-B 候选与标注包

- 仅在 Pilot1-A 通过后生成 `36` 条 `ANNOTATION_CANDIDATE / NOT_ADJUDICATED / NOT_BENCHMARK`：clean/current `12`、poison mutation `12`、matched hard negative `12`。
- `HKP-1..4 × S1..3` 的 12 种组合各 `1` 条；MutationSpec 只使用冻结 allowlist。
- 生成 Fact and Version Review 与 Stealth and Naturalness Review 两个独立 Git-external 包；每包 `36` 行，使用匿名 deterministic sample ID，隐藏 attack type、候选标签、mutation operation 和预期结论。
- `HUMAN_ANNOTATION = NOT STARTED`；`ANNOTATION_AGREEMENT = NOT ESTABLISHED`，没有伪造 annotator 或 adjudication。

### PILOT1 证据、结论边界与当前门

- `S6.1-P1-PILOT1 = COMPLETED_PENDING_REVIEW / REAL_PUBLIC_SOURCE_FEASIBILITY_ONLY`。
- Evidence index `17/17 PASS`；schema、group/split readiness、exact/normalized/identity leakage、packet index 和 raw-artifact exclusion 均通过。`SemanticNearDuplicateScanner = NOT_IMPLEMENTED / FAIL_IF_REQUIRED` 保持诚实披露。
- 本轮只证明公开来源到现有 schema、版本关系、候选与可执行盲化包的工程可行性；不证明 Benchmark 已构建、标注一致性、Detector 或 Option B 有效性，也没有论文指标。
- 持续状态：`240_GROUP_PILOT = NOT APPROVED / NOT STARTED`；Dataset `NOT FROZEN`；Detector `NOT IMPLEMENTED`；Retrieval Intervention Effectiveness `NOT ESTABLISHED`；Training/Formal Experiment `NOT STARTED`；Our Method Result `NONE`。
- 下一门且唯一下一门：项目需求提出人审查真实来源和标注包，然后决定是否启动真实双人标注与小规模一致性 Pilot；`Auto Continue = NO`。

### PILOT1 最终验收与 PILOT2 标注模式

- `S6.1-P1-PILOT1 = HUMAN_ACCEPTED / REAL_PUBLIC_SOURCE_AND_PACKET_FEASIBILITY_ONLY / CLOSED`。
- `ANNOTATION_MODE = TWO_INDEPENDENT_ANNOTATORS_WITH_OWNER_ADJUDICATION`：两名标注人独立提交，提交前不得讨论；owner 只在双方结果锁定后的 disagreement-only packet 上盲化仲裁。
- `S6.1-P1-PILOT2 = APPROVED_FOR_HUMAN_ANNOTATION_AGREEMENT_PILOT`，仅限现有 36 条候选；不批准 240-group Pilot、Dataset freeze、Detector、Retrieval Intervention effectiveness、Training 或 Formal Experiment。

### PILOT2-KIT 身份、结构与验证

- 从 corrected_02 manifest 自动解析原始 Fact/Version 与 Stealth/Naturalness packet；summary SHA `4952f166...ea6`、evidence `17/17`、候选 `36 = 12+12+12`、packet `36/36`、HKP×stealth `12/12` 全部通过。
- 生成 coordinator、6 条纯合成 training、A/B 各两个 Phase ZIP 和 owner-only 映射/空白仲裁模板；所有权威 CSV 为 UTF-8 BOM，response columns 全空。
- Phase 1 不含 official URL 或事实答案字段，并以 `assigned_stealth_level` 替代不可回答的 `stealth_level_reasonable`；Phase 2 才显示 official URL。
- Annotator ZIP 无 evaluator-only 字段、其他标注人文件或 owner-only mapping；ZIP safety、kit index `17/17`、package validation `15/15` 与 outer sidecar 全部通过。
- Outer ZIP SHA256：`a3c884ba313670aaeb78c3674e6c214434ae59576f99cc313fe4c4085eac6463`。

### PILOT2 当前状态与人工顺序

- `S6.1-P1-PILOT2 = ANNOTATION_KIT_PREPARED_PENDING_HUMAN_EXECUTION`。
- `HUMAN_ANNOTATION = AUTHORIZED / NOT STARTED`；`ANNOTATION_AGREEMENT = NOT ESTABLISHED`；未计算 disagreement，未执行 adjudication。
- 严格顺序：同时提供 training；分别发 A/B Phase 1；分别回收并记录 SHA256；两份 Phase 1 都锁定后才分别发 Phase 2；回收 Phase 2 后停止，等待新的验证/一致性计算授权。
- 继续保持：240-group Pilot `NOT APPROVED / NOT STARTED`；Dataset `NOT FROZEN`；Detector `NOT IMPLEMENTED`；Retrieval Intervention Effectiveness `NOT ESTABLISHED`；Training/Formal Experiment `NOT STARTED`；Our Method Result `NONE`。

### PILOT2 Return Preflight Owner Correction — 2026-08-27

- A/B Phase 1 与 Phase 2 returns 已收到。原 coordinator 登记时间曾导出“A Phase 2 先于 A Phase 1 回收”的 preflight
  推断；该原登记、原时间、原推断和原工作簿全部保留，不回写历史。
- 项目需求提出人通过 PODR-063/OR-025 确认实际顺序为 A Phase 1 完成提交、coordinator 回收锁定、之后才发 A Phase 2；
  A 在 Phase 1 期间未看到 Phase 2。故 `BLINDNESS_SUBISSUE = RESOLVED_BY_OWNER_CONFIRMED_ACTUAL_DISTRIBUTION_ORDER`。
- The original blind-contamination inference was based on incorrect registration metadata and is superseded by owner-confirmed
  actual distribution order. `REGISTRATION_TIMESTAMP_STATUS = INCORRECT_RECORDING / DOCUMENTATION_DEFECT_ONLY`。
- 当前 blocker 不是不可逆盲法污染，而是
  `PILOT2-RETURN-PROTOCOL-BLOCKER-01 = PROTOCOL_AND_ANNOTATION_SCHEMA_BLOCKER`。
- Open：registration metadata correction/evidence binding、Schema V2 applicability/enumeration、四份 declaration、GB18030/UTF-8
  BOM、B headers/time/professional lookup、lookup source type classification。
- `FORMAL_AGREEMENT = PENDING_SCHEMA_V2_REREVIEW_AND_RETURN_VALIDATION`；Round 1 可继续作为正式 agreement 候选，但本次不
  计算 agreement、不生成 disagreement、不执行 adjudication。
- 后续优先路线为 `ANNOTATION_SCHEMA_V2 + A/B INDEPENDENT RE-REVIEW`，不是强制使用新标注人；该路线仍需独立批准并冻结
  contract。Auto Continue = `NO`；不修改 raw returns，不联系 5090。

### PILOT2 Annotation Schema V2 与 A/B Round1 独立复核包 — 2026-08-27

- 项目需求提出人通过 PODR-064/OR-026 保留 `PILOT2_ROUND1_RAW = PRESERVED_IMMUTABLE`，批准本机实现 Schema V2 和
  A/B 基于本人 V1 的独立复核包；不使用 C/D，不重跑 240-group Pilot。
- 四份 raw ZIP、原 GB18030、列名/缺失/声明/lookup log、registration defect、preflight workbook 与原推断全部 hash-bound
  且未修改；owner correction 继续独立保存。
- V2 冻结四值语义、present/correctness applicability、authority proposition 边界、overall fact 决策树、Phase1 S1/S2/S3、
  lookup source enum、process missing sentinel、逐字段 KEEP/REVISE 与 retrospective declaration。
- 四包只含各自标注人的 V1 只读参考和空白 V2 表；全部 V2 CSV 为 UTF-8 BOM；A/B sample/claim identity 各阶段 `36/36`，
  no-peer/no-owner-only/no-private-intent leakage 通过。
- Git-external 交付键为 `LLMGuard-Handoff/paper1_pilot2_round1_rereview_v2_20260827`；四 ZIP SHA256 见
  [Schema V2 记录](../s6_1_p1_pilot2_annotation_v2.md)。本机 staging 到 E 盘 19 个文件逐 SHA 一致。
- 当前：`ANNOTATION_SCHEMA_V2 = IMPLEMENTED`；`A_B_REREVIEW = READY_FOR_HUMAN_EXECUTION`；
  `FORMAL_AGREEMENT_V2 = NOT_YET_ESTABLISHED`。不得计算 agreement、生成 disagreement、仲裁或推进 Dataset/Detector/
  Training/Formal Experiment。
- 下一门：A/B 独立填写各自 Phase1/Phase2 V2、change log 和 retrospective declaration；coordinator 锁定四份 return
  SHA256 后停止，等待 owner 单独批准 return validation/agreement。Auto Continue = `NO`。

### PILOT2 Annotator-Friendly Targeted Re-review — 2026-08-27

- 项目需求提出人通过 `PODR-065 / OR-027` 将完整 V2 的机械全字段重做收敛为问题驱动定向复核；raw、preflight、
  owner correction 与完整 V2 tree 全部保持不可变。
- Phase1 每人仅复核 `locally_detectable`、`cross_document_evidence_needed`、`assigned_stealth_level`；Phase2 每人仅
  复核 version/history/authority 三组 present/correctness 和 `overall_fact_status`。
- Phase1 naturalness/topic/confidence 与 Phase2 claim-match/fact-changed/confidence/evidence 保持本人 V1 只读；B 另补
  21 个 lookup-used 缺失与 1 个 search-engine source type，历史 time 不补造。
- 工作量从完整 V2 的每人 576 个实质样本×字段任务降为 360 个，减少 216 / 37.5%；A 总任务 360，B 含 process fixes
  总任务 382；每人另有两个一次性阶段声明。
- Git-external 交付键 `LLMGuard-Handoff/paper1_pilot2_targeted_rereview_20260827`；四个主 XLSX 和 exchange CSV 经
  own-V1-only、BOM、dropdown、readonly、formula、dependency、render 和 `17/17` copy SHA 检查。
- 严格顺序：A/B targeted Phase1 分别发放 -> 双方 return 分别 hash-lock -> A/B targeted Phase2 分别发放 -> 双方
  return 分别 hash-lock -> STOP。之后仅可在独立批准下依次进入 return validation、formal agreement analysis、必要
  owner adjudication 和 Ground Truth candidate lock。
- 当前：`TARGETED_REREVIEW_KIT = READY_FOR_HUMAN_EXECUTION`；`FORMAL_AGREEMENT_V2 = NOT_YET_ESTABLISHED`；
  Dataset/Detector/Training/5090/Formal Experiment 未推进。Auto Continue = `NO`。

### PILOT2 Targeted Re-review Correction 01 — 2026-08-28

- 项目需求提出人报告 A Phase1 已完成；本机观测 SHA256
  `100cffe2b81a23f3a65ade5ba712cd7aeefcfc56c600dae68f2b0241af36737f`，不重新生成、不覆盖、不放入更正包，待 return validation/hash lock。
- 查明 B Phase1 的 108 个 `[V1_ABSENT]` 是带中文后缀的历史列名没有被生成器识别，并非原标注缺失；B Phase2
  `version_relation_correct` / `authority_matches` 也有同类映射缺陷；A Phase2 通过同类检查。
- 修复器采用白名单 alias 归一化，对非预期缺失或 alias 冲突 fail closed。B Phase1 旧值恢复 `108/108`；A/B Phase2 仅三个
  真正新增的 `*_present` 字段标为 absent。
- 根据 PODR-066/OR-028，首页冻结 owner 的 version context 与 stealth 决策顺序；最终人工轮只有 B Phase1、A Phase2、B Phase2。
  这三份 return 是高优先级/高权重有效性证据候选，不是已验收 Ground Truth。
- Git-external 交付键 `LLMGuard-Handoff/paper1_pilot2_targeted_rereview_correction01_20260828`；只含三份待填 XLSX/CSV 及追加式协调/
  manifest 记录。12/12 sheets 视觉 QA、公式扫描、下拉/只读保护和映射回归通过。
- 三份人工 return 后固定路由：return validation/hash lock -> owner-approved agreement -> 仅必要仲裁 -> Ground Truth candidate
  acceptance -> 另行批准标注后实验准备。不自动计算 agreement，不启动 Dataset/Detector/Training/5090/Formal Experiment。Auto Continue = `NO`。

### Prospective Candidate Minimum Self-Containment Gate — 2026-08-28

- `PODR-067 / OR-029` 仅对未来新建或新引入的候选生效；当前 Pilot1/Pilot2 候选、raw returns 与 targeted returns 不修改、不推翻、不重新解释。
- 法律/政策/制度/标准的事实主体必须可从候选文本本身唯一识别；裸指代或需依赖隐含上下文时固定为 `BROKEN_CANDIDATE / MISSING_CONTEXT`。
- 该门先于人工标注、agreement、adjudication 和 Dataset freeze。失败候选只能补全主体后作为新候选重新审核，或剔除。
- 本机增加 prospective admission record 与 formal-Benchmark fail-closed guard；本次不执行新候选生成、数据重写或实验。Auto Continue = `NO`。

### PILOT2 Post-Annotation Validation and Agreement — 2026-08-31

- 项目需求提出人通过 `PODR-068 / OR-030` 确认四份 targeted return 已完成，批准执行 return validation、A/B V2 formal
  agreement 与 disagreement classification；不再要求第三轮全量标注。
- A/B Phase1 原声明的 `sample_id_changed=YES` 由 owner 更正为有效过程值 `NO`，只追加解释，不改原 XLSX。
- 权威输入 SHA256：A1 `9e301816...0424`、A2 `b7865999...5096`、corrected B1 `f4e1864e...2c8d`、B2
  `0572a0c...0989d`。20260827 目录中未填写的旧 B1 不作为 return。
- `RETURN_VALIDATION = PASS_FOR_AGREEMENT_WITH_NON_SEMANTIC_DEFECTS`；V2 值完整且合法，已知 V1 映射显示、空 reason、
  header 与 process metadata 缺陷保留为派生 validation evidence，不自动修正。
- `FORMAL_AGREEMENT_V2 = COMPLETED_ON_A_B_V2_CURRENT_VALUES`；conditional correctness 仅在双方 present=YES 子集计算。
  结果有 47 条 A/B 分歧及 37 条 schema-logic 冲突。
- Git-external 最小 packet 只含 26 个需判断候选、84 条问题，位于
  `LLMGuard-Handoff/paper1_pilot2_post_annotation_20260831`；workbook SHA256 `67081c0e...d363a`，index `11/11`。
- 当前：`OWNER_ADJUDICATION = REQUIRED / NOT_EXECUTED`；`GROUND_TRUTH_CANDIDATE = NOT_GENERATED`；Dataset、Detector、
  Training、5090 与 Formal Experiment 未启动。下一门是 owner 只裁决 packet 中的 26 个候选。Auto Continue = `NO`。

### PILOT2 Owner Adjudication Closure Attempt — 2026-08-31

- 项目需求提出人通过 `PODR-069 / OR-031` 批准读取完成版 owner packet；仅在 validation PASS 后生成 Pilot-only Ground
  Truth、关闭 Pilot2 并进入本机小规模 Pilot3 signal diagnostic。
- 唯一 workbook SHA256 为 `cf47a6c3...dcb1`，大小 `42,081` bytes；84/84 issue 和 26/26 candidate 完成度通过，
  PENDING 为 0；owner workbook 未保存或修改。
- 四个 candidate 的 owner final value 仍存在非法枚举或同字段冲突：`C-3ed6...`、`C-6fe04...`、`C-f73d...`、
  `C-fda2...`。本机不得根据重复行、rationale 或推荐值替 owner 选择。
- Git-external 最小 blocker evidence 位于 `LLMGuard-Handoff/paper1_pilot2_closure_20260831`，index `5/5`；只要求
  owner 重新确认四个候选，不重新标注 A/B。
- 当前：`OWNER_ADJUDICATION_CONSISTENCY_BLOCKER / HUMAN_DECISION_REQUIRED`；Ground Truth 未生成；Pilot2 未关闭；
  Pilot3 未开始；Dataset/Detector/Training/5090/Formal Experiment 均未推进。Auto Continue = `NO`。

### PILOT4 Signal Repair and Balanced Pre-annotation — 2026-09-01

- `PODR-071 / OR-033` 批准本机吸收 Pilot1–3 的数据与标注教训，先冻结唯一 canonical
  [future dataset rules](../human/annotation_lessons_learned_and_future_dataset_rules.md)，再执行 field-schema gate、G1–G14
  candidate gate、结构化 Temporal-Version/Provenance 修复和语义近重复检查。
- 24 个独立公开中文主体构成 24 个 matched triplets、72 条 `PREANNOTATION_CANDIDATE` 和 48 条自包含 query；Clean /
  Poison intent / matched Hard Negative 为 `24/24/24`，HKP×intended-S 的 12 个单元各 2 条，4 个领域各 6 triplets，
  SHORT/MEDIUM/LONG 各 8 triplets。
- authority-applicable `12/24` triplets，temporal-applicable `12/24`；六类 hard negative 各 4 条；72/72 主体唯一性、
  self-containment、G1–G14、四轮 QA、source reverse-check 与跨独立组 near-duplicate blocker 均 PASS。
- Git-external 证据键：`LLMGuard-Handoff/paper1_pilot4_preannotation_20260901`；Owner workbook 只抽样 12 条，尚未发给
  A/B。72 条不是 Ground Truth、Formal Benchmark 或 frozen Dataset。
- 历史状态：`S6.1-P1-PILOT4 = BALANCED_DIAGNOSTIC_SET_READY_FOR_OWNER_PREFLIGHT / PREANNOTATION_ONLY /
  NO_HUMAN_DISTRIBUTION`。第一次 owner preflight 后该快照由下方 targeted repair 状态取代；原 package 与证据保留。

### PILOT4 Targeted Repair and Second Owner Preflight — 2026-09-01

- 项目需求提出人通过 `PODR-072 / OR-034` 将第一次 Owner Preflight 判定为 `TARGETED_CORRECTION_REQUIRED`；提交
  `a843697` 和原 Git-external package 作为 failed-but-useful history 保持不可变。
- 定向修复 mutation semantic alignment、stealth evidence path、自然度/evidence echo、实验 meta-language、Phase1/Phase2/
  owner visibility、claim-derived applicability、serialized-artifact G1–G14、independent Round D 与 Hard Negative evidence chain。
- 24 个主体继续形成 24 个 repaired matched triplets 和 72 条候选；class intent `24/24/24`；12 个 HKP×intended-S
  generation cells 各 2；Pilot4 仍是四领域历史覆盖，不得改写成正式五领域。
- 修复实现身份为 `cad3b2b2c19dcef6c118e4163f705b3ec05713e1`；新 Git-external evidence root 为
  `paper1_pilot4_preannotation_repair_20260901`，含 repaired 12-row second Owner preflight。
- 当前：`PILOT4_BALANCED_SET_REPAIRED / READY_FOR_SECOND_OWNER_PREFLIGHT / PREANNOTATION_ONLY /
  NO_HUMAN_DISTRIBUTION`。唯一下一门为 Owner 审查 repaired 12 rows；不得自动接受、发 A/B、运行 agreement/
  adjudication、生成 Ground Truth、进入 240-group、冻结 Dataset、实现/训练正式 Detector、联系 5090 或运行正式实验。

### Paper 1 Human Documentation Integration — 2026-09-01

- `GOV-P1-HUMAN-DOCS-INTEGRATION-01` 仅执行 `DOCUMENTATION_STRUCTURE_AND_CONTEXT_INTEGRATION`，建立 5/15/30 分钟人类
  主入口、Research/Protocol authority、Agent/Audit/Evidence 分层、README 路由和 document inventory。
- 项目负责人确认未来正式 Paper 1 五领域：Enterprise HR、Finance、Information Security、Procurement and R&D、
  Education and Research；计划矩阵为 `5×4×3×4=240` independent groups，约 720 derived candidates，均未执行/未生成/未冻结。
- 本文档整理不改变任何 Pilot4 data/workbook/evidence 或实验状态。下一人工动作仍然只有 second Owner preflight；
  Auto Continue = `NO`。

### PILOT4 Final Preannotation Repair and Final Owner Preflight — 2026-09-01

- `PODR-074 / OR-036` 将第二次 Owner Preflight 登记为 `TARGETED_REPAIR_REQUIRED`；首轮 `a843697` 与第二轮 `cad3b2b`
  以及两棵 Git-external evidence tree 保持不可变。
- Repair-02 对八个 S3 poison 实施 evidence-necessity gate：任一直接证据单元均不足、联合证据才充分；四个 Owner 明确
  指出的 S3 例子全部改为跨法律、上下位法、前后版本或 repost/promulgation 关系。
- 八个 S1 poison 只保留自然内部冲突，删除显式错误提示；全部 72 条从最终 Phase1 文本重算字符数，三档各 8 个
  triplet，实际范围 SHORT `36–65`、MEDIUM `73–100`、LONG `143–165`。
- 24 matched triplets / 72 candidates 保持 class intent `24/24/24`，12 个 HKP×S cell 各 2；六类 Hard Negative 各 4；
  跨独立组完整句复用、高 n-gram overlap、非目标 claim parity、source verification、G1–G14、Round D 和 duplicate gate
  全部 PASS。
- 新 evidence root 为 `paper1_pilot4_preannotation_repair02_20260901`；最终 Owner workbook 含 16 行：12 Poison、2 Clean、
  2 Hard Negative。当前只允许 Owner final review，不得发 A/B 或推进任何下游实验。
- 当前：`PILOT4_FINAL_PREANNOTATION_READY_FOR_OWNER_REVIEW / NO_HUMAN_DISTRIBUTION`；Auto Continue = `NO`。

### PILOT4 Data and Annotation Protocol Quality Convergence — 2026-09-01

- Machine / role: `本机 / LOCAL_CONTROL_PLANE`；不联系 5090，不执行模型、GPU 或正式实验。
- Historical preservation: `a843697`、`cad3b2b`、`871aecf` 及对应 Git-external evidence 均保持不可变；本轮使用新的
  `paper1_pilot4_quality_convergence_20260901` additive namespace。
- Newly detected systemic blockers: Phase1 target-field leakage、为覆盖矩阵强造跨文档关系、S1 只校验片段存在、来源
  verification 只哈希生成命题、Hard Negative 复用生成支持文本。受影响候选按新规则全量复核；未删除失败历史。
- Repair: Phase1 收敛为 11 个盲法字段；Phase2 冻结最小充分 evidence path；隐蔽等级改为系统派生。16 条链发生本地文本
  修订，所有 24 Hard Negative 重新绑定实际官方来源；`forced_artificial_constructs=0`。
- QA: Phase1 leakage、primary subject、realism、full semantic audit 均 `72/72 PASS`；S1/S2/S3 分别 `8/8`；Hard Negative
  `24/24`；64 个唯一来源证据单元由实际 HTTP/PDF 内容、字节 SHA256、支持摘录 SHA256 和锚点验证。
- Schema: `PILOT4_ANNOTATION_SCHEMA_V3_CANDIDATE` 含 28 fields、53-row exhaustive truth table；每个人工字段有 13 类
  adversarial ambiguity case，alternative acceptable encoding 为 0。
- Dry-run: 12 条跨 HKP/S/length/domain/class 小样本；生成 Phase1、Phase2、Field Guide V3 三个 XLSX，8 个 Sheet 全部
  render/inspect/formula-scan PASS；`annotator_distribution=NO`。
- Final status: `PILOT4_QUALITY_CONVERGED / READY_FOR_OWNER_ACCEPTANCE_REVIEW / NO_HUMAN_DISTRIBUTION`。
- Next gate: Owner 审查完整 72-row human-readable artifact、Schema V3 candidate 与 dry-run 工作簿，并明确接受或指出新
  blocker。Auto Continue `NO`；不自动进入 A/B、agreement、adjudication、Ground Truth、240-group、Dataset freeze、
  Detector、Training、5090、Formal Experiment 或 Paper Result。

### PILOT4 Evidence Pool Repair and Schema V3.1 Finalization — 2026-09-01

- Task: `PILOT4-EVIDENCE-POOL-REPAIR-01`; machine `本机`；不联系 5090，不启动任何正式实验。
- Preserved input: 72 quality-converged candidate texts/IDs、24 triplets/independence groups、24/24/24 intent、64 frozen
  verified source units、candidate/source SHA256 与 full72 semantic audit 均不变；未提交的 V3.1 工作没有 reset 或覆盖。
- Blocker: 原 55/72 candidates、23 triplets 的 visible E1/E2 URL 重复。为每个受影响 triplet 追加 1 个 relevant、official、
  actually verified companion；23/23 HTTP 200 与 anchor PASS，URL/content hash/document identity 全唯一且不同于 frozen source。
- Evidence Pool: 72/72 各有两个 distinct verified source units；duplicate URL/content/document/excerpt/identity 为 0；无
  answer/minimum-path/class/HKP/intended-S 泄漏；A/B 使用独立 deterministic order，72/72 slot positions 交换。
- Schema V3.1: Phase1 仅 4 个 manual fields，Phase2 仅 7 个；version/authority 统一最终四值；claim 本身歧义由
  `phase2_issue=CANDIDATE_AMBIGUOUS` 表达，不能伪装为 evidence insufficiency；derived stealth/evidence metadata 全部 system-only。
- UI/documentation: machine fields/enums English canonical，中文只作必要解释，不增加 bilingual reverse mapping；生成
  Phase1、Phase2、Field Guide 三个 V3.1 XLSX，10/10 Sheets render/inspect/formula/visual QA PASS。
- Validation: SIM_A/SIM_B ambiguity `0`；full72 answerability `72/72 PASS`；return validator PASS；targeted tests `23 passed`。
- Evidence root: additive `LLMGuard-Handoff/paper1_pilot4_evidence_pool_repair_20260901`；manifest `53/53`，SHA256
  `180307ac3e24f7b82ba2f1bfb94417304286aa61a7de3556339f61b5f1640f64`；不覆盖任何历史 evidence tree。
- Final status: `PILOT4_ANNOTATION_PROTOCOL_READY_FOR_OWNER_ACCEPTANCE / NO_HUMAN_DISTRIBUTION`。
- Next gate: Owner 审查 V3.1 final review、三份 workbook、companion source QA 与 full72/validator QA，然后明确接受协议或
  指出新 blocker。即使接受，也必须另行批准 A/B distribution；Auto Continue `NO`。

### PILOT4 External Label-blind Owner Review Packet — 2026-09-02

- Owner authority: `PODR-077 / OR-039`; `PILOT4_OWNER_ACCEPTANCE_REVIEW = BLIND_EXTERNAL_REVIEW_REQUIRED`。本轮不登记
  protocol acceptance，继续 `NO_HUMAN_DISTRIBUTION`。
- Historical correction: c1b Full72 reviewer 可用 `sample_id` 查询六组硬编码 expected-label sets，因此重分类为
  `SAMPLE_ID_LABEL_LOOKUP_CONTAMINATED_REVIEW / NOT_ACCEPTABLE_AS_EXTERNAL_LABEL_BLIND_EVIDENCE`。c1b、b705cc、locked
  review、attempt01 mismatch、final comparison、workbooks 和历史 evidence tree 均保持不可变。
- Blind-side isolation: 72 个 HMAC 一次性不透明 ID；owner-only mapping 单独 hash-bound；外部 formatter 只读已脱敏 staging，
  不加载 expected contract、candidate kind、HKP、stealth、HN subtype、target field、owner-only 或任何 label set。
- Source title provenance: 56 个唯一 official URL 实际获取；144/144 可见 slot 的标题只来自 HTML title/H1、official document
  heading 或 PDF heading，绑定 source snapshot/hash；manual override 和 source-identity fallback 均为 0。
- Packet/guide: exactly four external files；72 rows；Phase1 4 fields + Phase2 7 fields；E1/E2 distinct `72/72`；11 个
  manual fields 各 6 个独立真实教学案例，共 66 个，未复用正式候选，机器未填写任何 semantic answer。
- Machine QA: original identity/design/Owner material leakage 0；blind ID `72/72` unique；matched-triplet adjacency 0；class/HKP/
  stealth/domain periodicity 0；candidate duplicate 与跨 triplet lexical near duplicate 0；mapping isolated。
- Final status: `PILOT4_EXTERNAL_BLIND_REVIEW_PACKET_READY / WAITING_FOR_EXTERNAL_OWNER_REVIEW /
  NO_HUMAN_DISTRIBUTION`。
- Next gate: Owner 将 external Markdown packet 交给隔离 GPT/Owner reviewer 并收回 `blind_review_id + 11 fields +
  reasoning`。mapping 解锁和 expected-contract comparison 是下一独立任务；Auto Continue `NO`。

### PILOT4 External Blind Review Phase Separation — 2026-09-02

- `PODR-078 / OR-040 / REL-2026-0045` 将 combined Phase1/Phase2 packet 保留为工程历史，但判定其不满足严格 LLM
  Phase1 blindness；当前项目上下文也因已接触候选和修复历史而不具备 external reviewer 资格。
- 正式 Pilot4 review 路径改为 `PHASE1 -> RETURN -> HASH LOCK -> PHASE2 RELEASE`。Phase1 只含 candidate 与四个判断字段，
  禁止 web/事实查证；Phase2 预构建但 withheld。
- Phase2 release 需要 receipt、schema valid、72/72 identity、SHA256 lock、immutable raw return 五项全部为真；当前均未完成，
  因而 fail closed。
- 当前状态：`PILOT4_EXTERNAL_BLIND_PHASE1_PACKET_READY / WAITING_FOR_EXTERNAL_PHASE1_REVIEW / PHASE2_WITHHELD /
  NO_HUMAN_DISTRIBUTION`。唯一下一动作是 Owner 在全新隔离上下文只提供 Phase1 packet 与 Phase1 guide。

### Paper 1 Mandatory Documentation Closeout — 2026-09-02

- `PODR-079 / OR-041 / REL-2026-0046` 建立所有 Paper 1 任务的持久收口门。任务必须同时通过 execution 和 documentation
  closeout；否则登记 `TASK_DOCUMENTATION_CLOSEOUT_BLOCKER`，不得称为全部完成。
- Human Ledger、Agent Ledger、Current Work State、Research Execution Log 为强制检查/同步项；Experiment Master、Owner
  Decision Register、Stage Process、Canonical Lessons、Research Plan Authority 与 README 按触发条件更新，禁止机械修改。
- 每轮执行 17-field closeout checklist 与 staleness gate。Pilot4 lessons 当前仅为
  `PROVISIONAL_PENDING_FINAL_ACCEPTANCE`；未来协议最终验收时另行执行 18-item promotion review。
- 本治理 Gate 不推进实验。Phase1 return 仍待外部隔离 reviewer，Phase2 仍 withheld；不得自动执行 review、接受协议、发
  A/B 或进入任何下游实验。

### PILOT4 External Phase1 Return Lock and Candidate-Defect Triage — 2026-09-02

- `PODR-080 / OR-042 / REL-2026-0047` 接收三份 byte-identical CSV：实际 E 盘来源带 `(1)` 后缀，无后缀合同示例不存在；
  raw size `5001`，SHA256 `59446c4be65b035be29528de81b6b8f8aa4113007df8fcac962fe4058a889261`。
- 新增 additive Git-external namespace `paper1_pilot4_external_blind_phase1_return_20260902`。raw CSV 按字节复制并保留
  sidecar；exact five-column schema、72/72 unique opaque IDs、enum 与 conditional reason 均 PASS；manifest `9/9`。
- Descriptive-only Phase1 counts：naturalness `NATURAL=63 / MINOR_ISSUE=8 / UNNATURAL=1`；局部冲突
  `NO=66 / YES=6 / UNCERTAIN=0`；issue `NONE=67 / MISSING_CONTEXT=2 / AMBIGUOUS_REFERENCE=2 / OTHER=1`。
- 5 条 issue rows 被抽取到 blind-ID-only Owner triage；该 artifact 不含 sample/private identity、Phase2、mapping、expected
  contract 或机器语义答案。原始 return 保持 immutable，Codex 不自动纠正候选。
- Phase2 release gate 扩展为六项：原五项 return-lock 事实均为 true；
  `PHASE1_CANDIDATE_DEFECT_TRIAGE_RESOLVED=false`。因此当前状态为
  `PILOT4_EXTERNAL_BLIND_PHASE1_RETURN_LOCKED / OWNER_DEFECT_TRIAGE_PENDING / PHASE2_WITHHELD /
  NO_HUMAN_DISTRIBUTION`。
- Next gate: Owner 只审查五条 blind-ID triage，并追加候选接受/修订重审/剔除替换处置。mapping unlock、expected
  comparison、Phase2 release、A/B 和全部下游任务仍需后续独立批准；Auto Continue `NO`。

### PILOT4 Phase1 Owner Defect Adjudication, Targeted Repair and Full72 Attempt2 — 2026-09-02

- `PODR-081 / OR-043 / REL-2026-0048` 将五条 Attempt1 blind-ID issue 全部登记为
  `REVIEWER_ISSUE_ACCEPTED / CANDIDATE_LOCAL_REPAIR_REQUIRED`。身份解锁仅限这五条；其余 67 条 mapping 不输出，full72
  expected comparison 不执行。
- 五条候选只做 source-backed、minimal、semantic-preserving repair。semantic/class/HKP/stealth/evidence-necessity parity 和
  source verification 均为 `5/5 PASS`；其余 67 条 candidate text 与源 JSONL 行保持 byte-identical。
- 新增 additive corpus `PILOT4_CANDIDATE_CORPUS_POST_EXTERNAL_PHASE1_REPAIR_V1`：67 unchanged + 5 repaired = 72。
  Attempt1 全部 raw/packet/return/mapping/triage evidence 保持 immutable，并分类为
  `VALID_DEFECT_DISCOVERY_REVIEW / NOT_FINAL_CORPUS_ACCEPTANCE_REVIEW`。
- Fresh Full72 Attempt2 使用 72 个新 opaque ID，Attempt1 ID 复用为 0；matched-triplet adjacency 0、无 2–12 行精确周期，
  五条 repaired candidate 未聚集，Phase1 URL/Evidence/Phase2/sample/label/expected leakage 均为 0。
- Attempt2 Phase2 已基于同一个 final corpus 和同一组新 ID 重建为 72 行/144 distinct evidence slots，但仍为
  `DO_NOT_RELEASE_BEFORE_ATTEMPT2_PHASE1_LOCK_AND_TRIAGE`。六项 release fact 当前全部为 false。
- 当前状态：`PILOT4_PHASE1_FIVE_DEFECTS_REPAIRED / EXTERNAL_BLIND_PHASE1_ATTEMPT2_PACKET_READY /
  WAITING_FOR_FRESH_EXTERNAL_PHASE1_REVIEW / PHASE2_WITHHELD / NO_HUMAN_DISTRIBUTION`。
- Next gate: Owner 在全新隔离 reviewer Project/context 中只发送 Attempt2 Phase1 packet Markdown 与 guide Markdown，收回
  72 行 return。当前不得执行 review、释放 Phase2、比较 expected contract、启动 A/B 或任何下游实验；Auto Continue `NO`。

### PILOT4 External Phase1 Attempt2 Lock and Controlled Phase2 Release — 2026-09-02

- `PODR-082 / OR-044 / REL-2026-0049` 授权本机锁定 fresh isolated reviewer 返回并在六项 release fact 全真且
  `phase1_issue != NONE` 为 0 时，在同一任务内只释放已预构建的 Attempt2 Phase2 packet。
- Canonical raw 为 `PILOT4_EXTERNAL_BLIND_PHASE1_ATTEMPT2_RETURN.csv`，`3117 bytes`，SHA256
  `1e5e81fee3825071a77d520c6da5cbfc4c2b59125aca0499cda6c7e2f363c9c5`；raw copy byte-identical，原始输入未重存、排序、
  改列或改换行。
- Schema/identity/enum/reason：exact 5 columns；72 rows；72 unique IDs；duplicate/blank/missing/unexpected 均 0；required
  reasons `7/7`。描述统计为自然度 `59/13/0`、局部内部冲突 `65/7/0`、候选缺陷 `72 NONE / 0 others`。
- 六项 Phase1 release fact 均为 true；repaired candidate 的 Attempt2 visible positions `1/26/51/61/70` 均为
  `phase1_issue=NONE`。该检查只使用 visible order，不解锁 mapping。
- Candidate Text Quality Gate 因 final72 的 fresh Full72 zero-defect return 关闭；这不是 expected-label accuracy acceptance。
  `EXPECTED_CONTRACT_LOADED=false`，`IDENTITY_MAPPING_UNLOCKED=false`。
- Phase2 packet 继续绑定 final corpus `PILOT4_CANDIDATE_CORPUS_POST_EXTERNAL_PHASE1_REPAIR_V1`、同一 72 IDs 与同一顺序；
  72 rows、144 distinct slots、144/144 title provenance、72/72 ID/text parity、hidden-label leakage 0。Codex 未执行 Phase2。
- Evidence root: Git-external `paper1_pilot4_external_blind_phase1_attempt2_return_20260902`；manifest `16/16`，aggregate
  SHA256 `1ad64c61778c6237845b665802ab60949a46cbdbcc01c333e0b72191e20b2f87`。Attempt1 与 Attempt2 preparation trees pre/post
  byte identity PASS。
- Current status: `PILOT4_EXTERNAL_BLIND_PHASE1_ATTEMPT2_LOCKED / PHASE1_CANDIDATE_QUALITY_GATE_PASS /
  PHASE2_RELEASED_TO_OWNER_FOR_EXTERNAL_REVIEW / WAITING_FOR_EXTERNAL_PHASE2_RETURN / NO_HUMAN_DISTRIBUTION`。
- Next gate: Owner 回到同一 `P1-Pilot4-External-Blind-Review-R2` Project / `Pilot4 External Blind Review — Phase1 Final`
  conversation，只发送 Phase2 packet Markdown 与 guide Markdown。Phase1 不得修改、重新解释或覆盖；mapping、expected
  contract、A/B、Dataset、Detector/Training、5090、Formal Experiment 全部保持关闭；Auto Continue `NO`。

### PILOT4 Final Phase2 Superseding Return and Expected Comparison — 2026-09-02

- `PODR-083 / OR-045 / REL-2026-0050` 撤回此前 23-row snapshot rescue 任务。First Phase2 raw 以 SHA256
  `d7aed1b2...f6cd` 永久保留为 access-limited process evidence；final raw 为 `16321 bytes` / SHA256
  `6f6cc042...92f1`，通过 exact 8 columns、72/72 IDs、enum、byte-identical immutable lock。
- First-to-final process diff 为 25 rows；23 条 `SOURCE_UNREACHABLE` 全部变化并在 final 降为 0；另外两条 refinement 是
  `BR-46BC044669` 与 `BR-C27BF02D5F`。因此 live URL 单次失败不能直接推断 official Evidence 无效；frozen snapshot + URL
  provenance 继续作为 provisional robustness lesson。
- Raw lock 时间 `2026-09-02T13:32:48.640267Z` 严格早于 mapping/expected load 时间
  `2026-09-02T13:42:28.686442Z`。Final72 mapping、post-repair corpus SHA `f530471e...8252d`、expected source 与五条
  `owner_or_expected_label_changed=false` repair binding 均通过 72/72 parity。
- Phase1 exact relevant-field agreement 为 `58/72`（naturalness 只作 QC）；Phase2 exact 为 `48/72`。43 field mismatches：
  M1=13、M2=16、M4=3、M5=6、M8=4、M9=1。`evidence_selection` 只作 process description，不计算 accuracy。
- `BR-18F1D39495` 分类为 `B / EVIDENCE_POOL_DESIGN_DEFECT`：给定两项 evidence 没有支持 2014 amendment 命题，reviewer 的
  `INSUFFICIENT_EVIDENCE` 有依据，expected 不自动胜出。
- Protocol recommendation 是 `RECOMMEND_TARGETED_REPAIR`，不是 Owner acceptance。16 条 overall primary label 的
  CURRENT/LEGITIMATE 边界、expected/minimum-evidence cases 与单条 evidence defect 必须追加式处置，不能回写 raw 或 expected source。
- Current status: `PILOT4_FINAL_BLIND_REVIEW_COMPARISON_COMPLETE / PROTOCOL_ACCEPTANCE_RECOMMENDATION_READY /
  OWNER_PROTOCOL_ACCEPTANCE_PENDING / NO_AB_DISTRIBUTION`。
- Next gate: Owner 冻结 primary-status 边界并决定是否批准定向修复。即使修复通过，protocol acceptance 与 A/B execution
  仍是两个独立后续 gate；GT、240-group、Dataset freeze、Detector/Training、5090、Formal Experiment 保持关闭。

### PILOT4 Protocol Targeted Repair and Fresh R3 Validation Packet — 2026-09-02

- `PODR-084 / OR-046 / REL-2026-0051` 批准只处理已冻结的 M2、M5、M8 和 `BR-18F1D39495`，不重跑 Full72，不覆盖历史。
- Guide V3.2 冻结字段局部决策顺序：先判事实冲突；正确命题只有在去掉版本/历史限定会丢失或实质改变含义时才是
  `LEGITIMATE_VERSION_OR_HISTORY`，否则为 `CURRENTLY_CONSISTENT`。六个独立教学例不复用正式候选。
- Expected V2 为追加版本，共 16 个字段变更（M2=6、M5=6、M8=4）；每项含旧值、新值、理由和来源。Expected V1、reviewer
  raw 和旧 mismatch taxonomy 不改写。
- Evidence Pool V2 只对 `BR-18F1D39495` 更换一项可直接证明 2014 修订关系的官方 E1；Expected 不因修复自动胜出，候选
  文本保持 byte-identical。
- R3 population 是 21 个 impacted candidates 加 16 个 matched controls，总计 37。新 opaque ID、随机顺序与 reviewer-only
  package 隔离 mapping、expected、旧 ID、taxonomy 和 control designation；E1/E2 同时提供 URL provenance 与 frozen snapshot。
- Current status: `TARGETED_REPAIR_COMPLETE / R3_VALIDATION_PACKET_READY /
  WAITING_FOR_FRESH_TARGETED_EXTERNAL_REVIEW / NO_AB_DISTRIBUTION`。R3 尚未执行，protocol 未接受。
- Next gate: Owner 在全新隔离 Project/conversation 中发送 reviewer packet、guide 与 snapshots，回收完整 CSV；后续任务必须
  先 immutable-lock raw，再加载 mapping/Expected V2。不得自动执行 R3、接受 protocol、发 A/B 或推进下游实验。
