# D1 Canary preclarification Phase2 review-run process incident V1

Task `P1-FORMAL240-D1-CANARY-PHASE2-RAW-LOCK-AND-V4_1-CLARIFICATION-01`. This is a **review-run-level** incident, not a candidate factual issue or a valid independent R1/R2 agreement result. It is an additive correction to the prior Phase1-lock *cross-phase continuity expectation*, not a rewrite of either Phase1 raw return or that task's historical lock event.

The Owner provided one composite pasted attachment, not two separate JSON files. Its exact 32,274 bytes (SHA256 `2227d5f7e988833a3ebd7b95fd11dcf72fe9fa73f5c486aa4654ff35f633d29b`) were locked in Git-external `paper1_formal240_d1_canary_phase2_preclarification_20260923`. The two reviewer JSON artifacts are **verbatim contiguous byte slices** from that composite, not normalized reconstructions or independently uploaded files. The composite also contains a previous Codex reply after the R2 array; that trailing text remains preserved in the full source but is not part of either reviewer JSON. Byte offsets and source/copy hashes are in the private slice-provenance manifest.

| Raw segment | Bytes | SHA256 | Validation |
| --- | ---: | --- | --- |
| `PAPER1_FORMAL_D1_CANARY_PHASE2_PRECLARIFICATION_R1_RAW_V1.json` | 14,925 | `487bbbb7b33bc18d6af8b58fc0de968a03260483b454574d938b7dd65ab573a8` | 24/24, exact ID/order/keys/enums, no duplicate/missing ID, all reasons present |
| `PAPER1_FORMAL_D1_CANARY_PHASE2_PRECLARIFICATION_R2_RAW_V1.json` | 15,745 | `1a7187859ddb0fd652ddfb4eaad5add732e5f1e6731d8639eba859c2f2f40092` | Same |

The R1 slice's `reviewer_note` explicitly says that this session's Phase1 role was R2 while the current assignment is R1, and that the batch cannot establish an independent R1 review relative to R2. On that first row, R1 raw also has `phase2_issue=OTHER`; the Owner and V4.1 classify the session mismatch as `REVIEWER_PROCESS_INDEPENDENCE_INVALID` in `review_run_metadata / blind_review_process_QA`, **not** as a candidate-level issue. Preserve the raw `OTHER` value and note; never silently change either. R1 is `NON_INDEPENDENT_AUXILIARY_QA_EVIDENCE` only. R2's raw is structurally valid and Owner reports a separate session, but the JSON alone does not prove its account/session lineage; record `VALID_PRECLARIFICATION_REVIEW_IF_SESSION_PROVENANCE_CONFIRMED`, not a complete independent pair. Neither return enters Canary acceptance.

The locked, **diagnostic-only** comparison (not independent agreement) finds: overall fact status `24/24` same; version status `18/24` same, six differences; authority status `20/24` same, four differences; minimum external evidence `24/24` same; actual evidence selection `19/24` same, five differences; phase2 issue `23/24` same, one difference (the process-contaminated `OTHER` row); accidental secondary error `24/24` same; evidence sufficiency `24/24` same. Fourteen distinct blind IDs have one or more field differences. The six version differences all contrast `PRESENT_CORRECT` with `NOT_PRESENT` on adoption/promulgation/order/publication wording without an explicit revision/effective/current-version transition. The four authority differences all contrast `PRESENT_CORRECT` with `NOT_PRESENT` for substantive institutional actors (administrative processing or medical proof), not an issuer/publisher claim. The five evidence-selection differences are descriptive use-path differences, not minimum-evidence disagreements. The per-ID raw values, text and reasons are in the Git-external comparison JSON. This pattern motivated a general field-scope clarification, **not** post-hoc correction of R1/R2 values.

Disposition: `CURRENT_PHASE2_RAW_LOCKED / R1_INDEPENDENCE_INVALIDATED / VALID_INDEPENDENT_AGREEMENT_NOT_COMPUTED / FRESH_R3_R4_REQUIRED / CANARY_ACCEPTANCE_PENDING`. No Candidate, Evidence, V4, Schema V4, previous reviewer return, Ground Truth, Human A/B package, Full48 candidate or model was changed. The next pair receives only the frozen packets and neutral V4.1 rules, never this incident or any earlier answers.
